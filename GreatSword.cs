using RPGGame;
namespace RPGGame{
    public class GreatSword : Weapon
    {   
        // GreatSword constructor
        public GreatSword(string name, string description, string rarity, int atk) : base(name, description, rarity, atk){

        }
    }
}