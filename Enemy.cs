namespace RPGGame
{
    public class Enemy : Living {

        // Enemy constructor
        public Enemy(string name, int health, int atk, int def) : base(name, health, atk, def) {
            
        }

        // Enemy methods

    }
}